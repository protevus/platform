# Enable mirrors for all tests
dart_args:
  - "--enable-mirrors"

# Run tests on the VM
platforms:
  - vm

# Test file patterns
filename: "*_test.dart"

# Test timeout
timeout: 30s

# Test reporter
reporter: expanded

# Test concurrency
concurrency: 4

# Coverage settings
coverage:
  include:
    - lib/**/*.dart
