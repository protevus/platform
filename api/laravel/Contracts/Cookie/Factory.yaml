name: Factory
class_comment: null
dependencies: []
properties: []
methods:
- name: make
  visibility: public
  parameters:
  - name: name
  - name: value
  - name: minutes
    default: '0'
  - name: path
    default: 'null'
  - name: domain
    default: 'null'
  - name: secure
    default: 'null'
  - name: httpOnly
    default: 'true'
  - name: raw
    default: 'false'
  - name: sameSite
    default: 'null'
  comment: '# * Create a new cookie instance.

    # *

    # * @param  string  $name

    # * @param  string  $value

    # * @param  int  $minutes

    # * @param  string|null  $path

    # * @param  string|null  $domain

    # * @param  bool|null  $secure

    # * @param  bool  $httpOnly

    # * @param  bool  $raw

    # * @param  string|null  $sameSite

    # * @return \Symfony\Component\HttpFoundation\Cookie'
- name: forever
  visibility: public
  parameters:
  - name: name
  - name: value
  - name: path
    default: 'null'
  - name: domain
    default: 'null'
  - name: secure
    default: 'null'
  - name: httpOnly
    default: 'true'
  - name: raw
    default: 'false'
  - name: sameSite
    default: 'null'
  comment: '# * Create a cookie that lasts "forever" (five years).

    # *

    # * @param  string  $name

    # * @param  string  $value

    # * @param  string|null  $path

    # * @param  string|null  $domain

    # * @param  bool|null  $secure

    # * @param  bool  $httpOnly

    # * @param  bool  $raw

    # * @param  string|null  $sameSite

    # * @return \Symfony\Component\HttpFoundation\Cookie'
- name: forget
  visibility: public
  parameters:
  - name: name
  - name: path
    default: 'null'
  - name: domain
    default: 'null'
  comment: '# * Expire the given cookie.

    # *

    # * @param  string  $name

    # * @param  string|null  $path

    # * @param  string|null  $domain

    # * @return \Symfony\Component\HttpFoundation\Cookie'
traits: []
interfaces: []
