name: Connector
class_comment: null
dependencies: []
properties: []
methods:
- name: connect
  visibility: public
  parameters:
  - name: config
  - name: options
  comment: '# * Create a connection to a Redis cluster.

    # *

    # * @param  array  $config

    # * @param  array  $options

    # * @return \Illuminate\Redis\Connections\Connection'
- name: connectToCluster
  visibility: public
  parameters:
  - name: config
  - name: clusterOptions
  - name: options
  comment: '# * Create a connection to a Redis instance.

    # *

    # * @param  array  $config

    # * @param  array  $clusterOptions

    # * @param  array  $options

    # * @return \Illuminate\Redis\Connections\Connection'
traits: []
interfaces: []
