name: AddQueuedCookiesToResponse
class_comment: null
dependencies:
- name: Closure
  type: class
  source: Closure
- name: CookieJar
  type: class
  source: Illuminate\Contracts\Cookie\QueueingFactory
properties:
- name: cookies
  visibility: protected
  comment: '# * The cookie jar instance.

    # *

    # * @var \Illuminate\Contracts\Cookie\QueueingFactory'
methods:
- name: __construct
  visibility: public
  parameters:
  - name: cookies
  comment: "# * The cookie jar instance.\n# *\n# * @var \\Illuminate\\Contracts\\\
    Cookie\\QueueingFactory\n# */\n# protected $cookies;\n# \n# /**\n# * Create a\
    \ new CookieQueue instance.\n# *\n# * @param  \\Illuminate\\Contracts\\Cookie\\\
    QueueingFactory  $cookies\n# * @return void"
- name: handle
  visibility: public
  parameters:
  - name: request
  - name: next
  comment: '# * Handle an incoming request.

    # *

    # * @param  \Illuminate\Http\Request  $request

    # * @param  \Closure  $next

    # * @return mixed'
traits:
- Closure
interfaces: []
