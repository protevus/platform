name: CookieValuePrefix
class_comment: null
dependencies: []
properties: []
methods:
- name: create
  visibility: public
  parameters:
  - name: cookieName
  - name: key
  comment: '# * Create a new cookie value prefix for the given cookie name.

    # *

    # * @param  string  $cookieName

    # * @param  string  $key

    # * @return string'
- name: remove
  visibility: public
  parameters:
  - name: cookieValue
  comment: '# * Remove the cookie value prefix.

    # *

    # * @param  string  $cookieValue

    # * @return string'
- name: validate
  visibility: public
  parameters:
  - name: cookieName
  - name: cookieValue
  - name: keys
  comment: '# * Validate a cookie value contains a valid prefix. If it does, return
    the cookie value with the prefix removed. Otherwise, return null.

    # *

    # * @param  string  $cookieName

    # * @param  string  $cookieValue

    # * @param  array  $keys

    # * @return string|null'
traits: []
interfaces: []
