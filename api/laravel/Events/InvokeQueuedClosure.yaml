name: InvokeQueuedClosure
class_comment: null
dependencies: []
properties: []
methods:
- name: handle
  visibility: public
  parameters:
  - name: closure
  - name: arguments
  comment: '# * Handle the event.

    # *

    # * @param  \Laravel\SerializableClosure\SerializableClosure  $closure

    # * @param  array  $arguments

    # * @return void'
- name: failed
  visibility: public
  parameters:
  - name: closure
  - name: arguments
  - name: catchCallbacks
  - name: exception
  comment: '# * Handle a job failure.

    # *

    # * @param  \Laravel\SerializableClosure\SerializableClosure  $closure

    # * @param  array  $arguments

    # * @param  array  $catchCallbacks

    # * @param  \Throwable  $exception

    # * @return void'
traits: []
interfaces: []
