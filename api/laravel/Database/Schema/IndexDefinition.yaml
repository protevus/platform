name: IndexDefinition
class_comment: '# * @method $this algorithm(string $algorithm) Specify an algorithm
  for the index (MySQL/PostgreSQL)

  # * @method $this language(string $language) Specify a language for the full text
  index (PostgreSQL)

  # * @method $this deferrable(bool $value = true) Specify that the unique index is
  deferrable (PostgreSQL)

  # * @method $this initiallyImmediate(bool $value = true) Specify the default time
  to check the unique index constraint (PostgreSQL)'
dependencies:
- name: Fluent
  type: class
  source: Illuminate\Support\Fluent
properties: []
methods: []
traits:
- Illuminate\Support\Fluent
interfaces: []
