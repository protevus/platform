name: MariaDbConnector
class_comment: null
dependencies:
- name: PDO
  type: class
  source: PDO
properties: []
methods:
- name: getSqlMode
  visibility: protected
  parameters:
  - name: connection
  - name: config
  comment: '# * Get the sql_mode value.

    # *

    # * @param  \PDO  $connection

    # * @param  array  $config

    # * @return string|null'
traits:
- PDO
interfaces: []
