name: When
class_comment: null
dependencies: []
properties: []
methods:
- name: __construct
  visibility: public
  parameters:
  - name: env
  comment: '# * An attribute to tell under which environment this class should be
    registered as a service.

    # *

    # * @author Nicolas Grekas <p@tchwork.com>

    # */

    # #[\Attribute(\Attribute::TARGET_CLASS | \Attribute::TARGET_METHOD | \Attribute::TARGET_FUNCTION
    | \Attribute::IS_REPEATABLE)]

    # class When

    # {

    # /**

    # * @param string $env The environment under which the class will be registered
    as a service (i.e. "dev", "test", "prod")'
traits: []
interfaces: []
