name: FileBinaryMimeTypeGuesser
class_comment: '# * Guesses the MIME type with the binary "file" (only available on
  *nix).

  # *

  # * @author Bernhard Schussek <bschussek@gmail.com>'
dependencies:
- name: InvalidArgumentException
  type: class
  source: Symfony\Component\Mime\Exception\InvalidArgumentException
- name: LogicException
  type: class
  source: Symfony\Component\Mime\Exception\LogicException
properties: []
methods:
- name: __construct
  visibility: public
  parameters:
  - name: cmd
    default: '''file -b --mime -- %s 2>/dev/null'''
  comment: '# * Guesses the MIME type with the binary "file" (only available on *nix).

    # *

    # * @author Bernhard Schussek <bschussek@gmail.com>

    # */

    # class FileBinaryMimeTypeGuesser implements MimeTypeGuesserInterface

    # {

    # /**

    # * The $cmd pattern must contain a "%s" string that will be replaced

    # * with the file name to guess.

    # *

    # * The command output must start with the MIME type of the file.

    # *

    # * @param string $cmd The command to run to get the MIME type of a file'
- name: isGuesserSupported
  visibility: public
  parameters: []
  comment: null
- name: guessMimeType
  visibility: public
  parameters:
  - name: path
  comment: null
traits:
- Symfony\Component\Mime\Exception\InvalidArgumentException
- Symfony\Component\Mime\Exception\LogicException
interfaces:
- MimeTypeGuesserInterface
