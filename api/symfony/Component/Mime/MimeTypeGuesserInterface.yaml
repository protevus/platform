name: MimeTypeGuesserInterface
class_comment: null
dependencies: []
properties: []
methods:
- name: isGuesserSupported
  visibility: public
  parameters: []
  comment: '# * Guesses the MIME type of a file.

    # *

    # * @author Fabien Potencier <fabien@symfony.com>

    # */

    # interface MimeTypeGuesserInterface

    # {

    # /**

    # * Returns true if this guesser is supported.'
- name: guessMimeType
  visibility: public
  parameters:
  - name: path
  comment: '# * Guesses the MIME type of the file with the given path.

    # *

    # * @throws \LogicException           If the guesser is not supported

    # * @throws \InvalidArgumentException If the file does not exist or is not readable'
traits: []
interfaces: []
