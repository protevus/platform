name: FixUrlProtocolListener
class_comment: '# * Adds a protocol to a URL if it doesn''t already have one.

  # *

  # * @author Bernhard Schussek <bschussek@gmail.com>'
dependencies:
- name: EventSubscriberInterface
  type: class
  source: Symfony\Component\EventDispatcher\EventSubscriberInterface
- name: FormEvent
  type: class
  source: Symfony\Component\Form\FormEvent
- name: FormEvents
  type: class
  source: Symfony\Component\Form\FormEvents
properties: []
methods:
- name: __construct
  visibility: public
  parameters:
  - name: defaultProtocol
    default: '''http'''
  comment: '# * Adds a protocol to a URL if it doesn''t already have one.

    # *

    # * @author Bernhard Schussek <bschussek@gmail.com>

    # */

    # class FixUrlProtocolListener implements EventSubscriberInterface

    # {

    # /**

    # * @param string|null $defaultProtocol The URL scheme to add when there is none
    or null to not modify the data'
- name: onSubmit
  visibility: public
  parameters:
  - name: event
  comment: null
- name: getSubscribedEvents
  visibility: public
  parameters: []
  comment: null
traits:
- Symfony\Component\EventDispatcher\EventSubscriberInterface
- Symfony\Component\Form\FormEvent
- Symfony\Component\Form\FormEvents
interfaces:
- EventSubscriberInterface
