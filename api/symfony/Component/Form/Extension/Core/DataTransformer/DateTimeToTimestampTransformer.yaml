name: DateTimeToTimestampTransformer
class_comment: '# * Transforms between a timestamp and a DateTime object.

  # *

  # * @author Bernhard Schussek <bschussek@gmail.com>

  # * @author Florian Eckerstorfer <florian@eckerstorfer.org>

  # *

  # * @extends BaseDateTimeTransformer<int|numeric-string>'
dependencies:
- name: TransformationFailedException
  type: class
  source: Symfony\Component\Form\Exception\TransformationFailedException
properties: []
methods:
- name: transform
  visibility: public
  parameters:
  - name: dateTime
  comment: '# * Transforms between a timestamp and a DateTime object.

    # *

    # * @author Bernhard Schussek <bschussek@gmail.com>

    # * @author Florian Eckerstorfer <florian@eckerstorfer.org>

    # *

    # * @extends BaseDateTimeTransformer<int|numeric-string>

    # */

    # class DateTimeToTimestampTransformer extends BaseDateTimeTransformer

    # {

    # /**

    # * Transforms a DateTime object into a timestamp in the configured timezone.

    # *

    # * @param \DateTimeInterface $dateTime A DateTimeInterface object

    # *

    # * @throws TransformationFailedException If the given value is not a \DateTimeInterface'
- name: reverseTransform
  visibility: public
  parameters:
  - name: value
  comment: '# * Transforms a timestamp in the configured timezone into a DateTime
    object.

    # *

    # * @param string $value A timestamp

    # *

    # * @throws TransformationFailedException If the given value is not a timestamp

    # *                                       or if the given timestamp is invalid'
traits:
- Symfony\Component\Form\Exception\TransformationFailedException
interfaces: []
