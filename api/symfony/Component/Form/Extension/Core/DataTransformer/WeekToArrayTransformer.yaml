name: WeekToArrayTransformer
class_comment: '# * Transforms between an ISO 8601 week date string and an array.

  # *

  # * @author Damien Fayet <damienf1521@gmail.com>

  # *

  # * @implements DataTransformerInterface<string, array{year: int|null, week: int|null}>'
dependencies:
- name: DataTransformerInterface
  type: class
  source: Symfony\Component\Form\DataTransformerInterface
- name: TransformationFailedException
  type: class
  source: Symfony\Component\Form\Exception\TransformationFailedException
properties: []
methods:
- name: transform
  visibility: public
  parameters:
  - name: value
  comment: '# * Transforms between an ISO 8601 week date string and an array.

    # *

    # * @author Damien Fayet <damienf1521@gmail.com>

    # *

    # * @implements DataTransformerInterface<string, array{year: int|null, week: int|null}>

    # */

    # class WeekToArrayTransformer implements DataTransformerInterface

    # {

    # /**

    # * Transforms a string containing an ISO 8601 week date into an array.

    # *

    # * @param string|null $value A week date string

    # *

    # * @return array{year: int|null, week: int|null}

    # *

    # * @throws TransformationFailedException If the given value is not a string,

    # *                                       or if the given value does not follow
    the right format'
- name: reverseTransform
  visibility: public
  parameters:
  - name: value
  comment: '# * Transforms an array into a week date string.

    # *

    # * @param array{year: int|null, week: int|null} $value

    # *

    # * @return string|null A week date string following the format Y-\WW

    # *

    # * @throws TransformationFailedException If the given value cannot be merged
    in a valid week date string,

    # *                                       or if the obtained week date does not
    exists'
traits:
- Symfony\Component\Form\DataTransformerInterface
- Symfony\Component\Form\Exception\TransformationFailedException
interfaces:
- DataTransformerInterface
- DataTransformerInterface
