name: UlidToStringTransformer
class_comment: '# * Transforms between a ULID string and a Ulid object.

  # *

  # * @author Pavel Dyakonov <wapinet@mail.ru>

  # *

  # * @implements DataTransformerInterface<Ulid, string>'
dependencies:
- name: DataTransformerInterface
  type: class
  source: Symfony\Component\Form\DataTransformerInterface
- name: TransformationFailedException
  type: class
  source: Symfony\Component\Form\Exception\TransformationFailedException
- name: Ulid
  type: class
  source: Symfony\Component\Uid\Ulid
properties: []
methods:
- name: transform
  visibility: public
  parameters:
  - name: value
  comment: '# * Transforms between a ULID string and a Ulid object.

    # *

    # * @author Pavel Dyakonov <wapinet@mail.ru>

    # *

    # * @implements DataTransformerInterface<Ulid, string>

    # */

    # class UlidToStringTransformer implements DataTransformerInterface

    # {

    # /**

    # * Transforms a Ulid object into a string.

    # *

    # * @param Ulid $value A Ulid object

    # *

    # * @throws TransformationFailedException If the given value is not a Ulid object'
- name: reverseTransform
  visibility: public
  parameters:
  - name: value
  comment: '# * Transforms a ULID string into a Ulid object.

    # *

    # * @param string $value A ULID string

    # *

    # * @throws TransformationFailedException If the given value is not a string,

    # *                                       or could not be transformed'
traits:
- Symfony\Component\Form\DataTransformerInterface
- Symfony\Component\Form\Exception\TransformationFailedException
- Symfony\Component\Uid\Ulid
interfaces:
- DataTransformerInterface
- DataTransformerInterface
