name: IntegerToLocalizedStringTransformer
class_comment: '# * Transforms between an integer and a localized number with grouping

  # * (each thousand) and comma separators.

  # *

  # * @author Bernhard Schussek <bschussek@gmail.com>'
dependencies:
- name: TransformationFailedException
  type: class
  source: Symfony\Component\Form\Exception\TransformationFailedException
properties: []
methods:
- name: __construct
  visibility: public
  parameters:
  - name: grouping
    default: 'false'
  - name: roundingMode
    default: \NumberFormatter::ROUND_DOWN
  - name: locale
    default: 'null'
  comment: '# * Transforms between an integer and a localized number with grouping

    # * (each thousand) and comma separators.

    # *

    # * @author Bernhard Schussek <bschussek@gmail.com>

    # */

    # class IntegerToLocalizedStringTransformer extends NumberToLocalizedStringTransformer

    # {

    # /**

    # * Constructs a transformer.

    # *

    # * @param bool        $grouping     Whether thousands should be grouped

    # * @param int|null    $roundingMode One of the ROUND_ constants in this class

    # * @param string|null $locale       locale used for transforming'
- name: reverseTransform
  visibility: public
  parameters:
  - name: value
  comment: null
- name: castParsedValue
  visibility: protected
  parameters:
  - name: value
  comment: '# * @internal'
traits:
- Symfony\Component\Form\Exception\TransformationFailedException
interfaces: []
