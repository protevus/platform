name: AsEventListener
class_comment: null
dependencies: []
properties: []
methods:
- name: __construct
  visibility: public
  parameters:
  - name: event
    default: 'null'
  - name: method
    default: 'null'
  - name: priority
    default: '0'
  - name: dispatcher
    default: 'null'
  comment: '# * Service tag to autoconfigure event listeners.

    # *

    # * @author Alexander M. Turek <me@derrabus.de>

    # */

    # #[\Attribute(\Attribute::TARGET_CLASS | \Attribute::TARGET_METHOD | \Attribute::IS_REPEATABLE)]

    # class AsEventListener

    # {

    # /**

    # * @param string|null $event      The event name to listen to

    # * @param string|null $method     The method to run when the listened event is
    triggered

    # * @param int         $priority   The priority of this listener if several are
    declared for the same event

    # * @param string|null $dispatcher The service id of the event dispatcher to listen
    to'
traits: []
interfaces: []
