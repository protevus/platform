name: FilenameFilterIterator
class_comment: '# * FilenameFilterIterator filters files by patterns (a regexp, a
  glob, or a string).

  # *

  # * @author Fabien Potencier <fabien@symfony.com>

  # *

  # * @extends MultiplePcreFilterIterator<string, \SplFileInfo>'
dependencies:
- name: Glob
  type: class
  source: Symfony\Component\Finder\Glob
properties: []
methods:
- name: accept
  visibility: public
  parameters: []
  comment: '# * FilenameFilterIterator filters files by patterns (a regexp, a glob,
    or a string).

    # *

    # * @author Fabien Potencier <fabien@symfony.com>

    # *

    # * @extends MultiplePcreFilterIterator<string, \SplFileInfo>

    # */

    # class FilenameFilterIterator extends MultiplePcreFilterIterator

    # {

    # /**

    # * Filters the iterator values.'
- name: toRegex
  visibility: protected
  parameters:
  - name: str
  comment: '# * Converts glob to regexp.

    # *

    # * PCRE patterns are left unchanged.

    # * Glob strings are transformed with Glob::toRegex().

    # *

    # * @param string $str Pattern: glob or regexp'
traits:
- Symfony\Component\Finder\Glob
interfaces: []
