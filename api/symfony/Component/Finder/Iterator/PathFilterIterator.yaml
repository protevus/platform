name: PathFilterIterator
class_comment: "# * PathFilterIterator filters files by path patterns (e.g. some/special/dir).\n\
  # *\n# * @author Fabien Potencier  <fabien@symfony.com>\n# * @author W\u0142odzimierz\
  \ Gajda <gajdaw@gajdaw.pl>\n# *\n# * @extends MultiplePcreFilterIterator<string,\
  \ SplFileInfo>"
dependencies:
- name: SplFileInfo
  type: class
  source: Symfony\Component\Finder\SplFileInfo
properties: []
methods:
- name: accept
  visibility: public
  parameters: []
  comment: "# * PathFilterIterator filters files by path patterns (e.g. some/special/dir).\n\
    # *\n# * @author Fabien Potencier  <fabien@symfony.com>\n# * @author W\u0142odzimierz\
    \ Gajda <gajdaw@gajdaw.pl>\n# *\n# * @extends MultiplePcreFilterIterator<string,\
    \ SplFileInfo>\n# */\n# class PathFilterIterator extends MultiplePcreFilterIterator\n\
    # {\n# /**\n# * Filters the iterator values."
- name: toRegex
  visibility: protected
  parameters:
  - name: str
  comment: '# * Converts strings to regexp.

    # *

    # * PCRE patterns are left unchanged.

    # *

    # * Default conversion:

    # *     ''lorem/ipsum/dolor'' ==>  ''lorem\/ipsum\/dolor/''

    # *

    # * Use only / as directory separator (on Windows also).

    # *

    # * @param string $str Pattern: regexp or dirname'
traits:
- Symfony\Component\Finder\SplFileInfo
interfaces: []
