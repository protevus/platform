name: MultiplePcreFilterIterator
class_comment: null
dependencies: []
properties: []
methods:
- name: __construct
  visibility: public
  parameters:
  - name: iterator
  - name: matchPatterns
  - name: noMatchPatterns
  comment: "# * MultiplePcreFilterIterator filters files using patterns (regexps,\
    \ globs or strings).\n# *\n# * @author Fabien Potencier <fabien@symfony.com>\n\
    # *\n# * @template-covariant TKey\n# * @template-covariant TValue\n# *\n# * @extends\
    \ \\FilterIterator<TKey, TValue>\n# */\n# abstract class MultiplePcreFilterIterator\
    \ extends \\FilterIterator\n# {\n# protected array $matchRegexps = [];\n# protected\
    \ array $noMatchRegexps = [];\n# \n# /**\n# * @param \\Iterator<TKey, TValue>\
    \ $iterator        The Iterator to filter\n# * @param string[]               \
    \ $matchPatterns   An array of patterns that need to match\n# * @param string[]\
    \                $noMatchPatterns An array of patterns that need to not match"
- name: isAccepted
  visibility: protected
  parameters:
  - name: string
  comment: '# * Checks whether the string is accepted by the regex filters.

    # *

    # * If there is no regexps defined in the class, this method will accept the string.

    # * Such case can be handled by child classes before calling the method if they
    want to

    # * apply a different behavior.'
- name: isRegex
  visibility: protected
  parameters:
  - name: str
  comment: '# * Checks whether the string is a regex.'
- name: toRegex
  visibility: protected
  parameters:
  - name: str
  comment: null
traits: []
interfaces: []
