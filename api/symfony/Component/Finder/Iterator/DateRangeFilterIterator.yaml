name: DateRangeFilterIterator
class_comment: '# * DateRangeFilterIterator filters out files that are not in the
  given date range (last modified dates).

  # *

  # * @author Fabien Potencier <fabien@symfony.com>

  # *

  # * @extends \FilterIterator<string, \SplFileInfo>'
dependencies:
- name: DateComparator
  type: class
  source: Symfony\Component\Finder\Comparator\DateComparator
properties: []
methods:
- name: __construct
  visibility: public
  parameters:
  - name: iterator
  - name: comparators
  comment: "# * DateRangeFilterIterator filters out files that are not in the given\
    \ date range (last modified dates).\n# *\n# * @author Fabien Potencier <fabien@symfony.com>\n\
    # *\n# * @extends \\FilterIterator<string, \\SplFileInfo>\n# */\n# class DateRangeFilterIterator\
    \ extends \\FilterIterator\n# {\n# private array $comparators = [];\n# \n# /**\n\
    # * @param \\Iterator<string, \\SplFileInfo> $iterator\n# * @param DateComparator[]\
    \                $comparators"
- name: accept
  visibility: public
  parameters: []
  comment: '# * Filters the iterator values.'
traits:
- Symfony\Component\Finder\Comparator\DateComparator
interfaces: []
